import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'cases',
  styleUrls: ['cases.css'],
  templateUrl: 'cases.html',
})
export class CasesComponent  { title = 'Cases'; }
